version: "2"
services:
  api:
    env_file:
    - .env
    build: .
    ports:
    - "8080:8080"
    depends_on:
    - mongo
    - redis
  mongo:
    image: mongo
    ports: 
    - "27017:27017"
    volumes:
    - data-volume:/data/db
  redis:
    image: "redis:latest"
volumes:
  data-volume: